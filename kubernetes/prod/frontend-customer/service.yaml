apiVersion: v1
kind: Service
metadata:
  name: frontend-customer
  namespace: framely-stage
  labels:
    app: frontend-customer
spec:
  type: ClusterIP
  selector:
    app: frontend-customer
  ports:
    - name: http
      port: 80
      targetPort: 3000
